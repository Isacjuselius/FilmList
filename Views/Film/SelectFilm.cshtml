@model IEnumerable<FilmList.Models.FilmDetails>

@{
    ViewData["Title"] = "Select film";
    var films = Model ?? Enumerable.Empty<FilmList.Models.FilmDetails>();
}

<form asp-controller="Film" asp-action="FilterFilm" method="post">
    <label for="genre">Filter by genre</label>

    <select id="genre" name="genre">
        <option value="0">Choose genre</option>
        <option value="1">Action</option>
        <option value="2">Drama</option>
        <option value="3">Comedy</option>
    </select>

    <button type="submit">Filter</button>

    <a asp-action="SelectFilm" asp-controller="Film">
        Show all
    </a>
</form>
<form asp-action="SelectFilm" method="get">
    <input type="text"
           name="searchString"
           placeholder="Sök film..."
           value="@Context.Request.Query["searchString"]" />

    <input type="checkbox"
        name="sorted"
        value="true"
        @(Context.Request.Query["sorted"] == "true" ? "checked" : "") />
        Sort A–Ö
    <button type="submit">Sök</button>
</form>

    

<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.FilmId)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.FilmTitle)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.GenreId)
            </th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in films) {
        <tr>
            <td>
                @Html.DisplayFor(model => item.FilmId)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.FilmTitle)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.GenreId)
            </td>

            <td>
                @Html.ActionLink("Edit", "EditFilm", new { id=item.FilmId }) |
                @Html.ActionLink("Details", "Details", new { id=item.FilmId }) |
                @Html.ActionLink("Delete", "DeleteFilm", new { id=item.FilmId })
            </td>
        </tr>
}
    </tbody>
</table>